# คู่มือติดตั้งระบบสมาชิก CPC–PSW Thailand

ระบบนี้ประกอบด้วย 2 ส่วน:
- **ฐานข้อมูล + backend**: Google ชีต + Google Apps Script (ทำหน้าที่เป็น API)
- **หน้าเว็บ (frontend)**: ไฟล์ HTML/CSS/JS ล้วน ๆ โฮสต์ฟรีบน GitHub Pages

---

## ส่วนที่ 1 — ตั้งค่า Google ชีตและ Apps Script

1. สร้าง Google ชีตใหม่ที่ [sheets.google.com](https://sheets.google.com) ตั้งชื่อ เช่น **"CPC-PSW Members DB"**
2. เมนู **ส่วนขยาย (Extensions) → Apps Script** (วิธีนี้จะสร้างสคริปต์แบบ "ผูกกับชีต" ให้อัตโนมัติ ซึ่งง่ายที่สุด)
3. ลบโค้ดตัวอย่างในไฟล์ `Code.gs` ออกทั้งหมด แล้ว **คัดลอกเนื้อหาทั้งหมด** จากไฟล์ `apps-script/Code.gs` ในโปรเจกต์นี้ไปวางแทน
4. **ถ้าคุณสร้างสคริปต์จาก [script.google.com](https://script.google.com) โดยตรง** (แบบ standalone ไม่ได้เปิดผ่านเมนู Extensions ของชีต) ต้องระบุ Spreadsheet ID ด้วย:
   - เปิด Google ชีต แล้วดู URL รูปแบบ `https://docs.google.com/spreadsheets/d/<SPREADSHEET_ID>/edit`
   - คัดลอกส่วน `<SPREADSHEET_ID>` มาวางแทนที่บรรทัด
     ```js
     const SPREADSHEET_ID = "ใส่_SPREADSHEET_ID_ของคุณที่นี่";
     ```
   - หากเปิดผ่านเมนู Extensions ในชีต (ข้อ 2) ไม่ต้องแก้บรรทัดนี้ก็ได้ ระบบจะหาชีตให้อัตโนมัติ
5. กดปุ่ม **บันทึก** (ไอคอนแผ่นดิสก์)
6. ที่แถบด้านบน เลือกฟังก์ชัน `setup` จากดรอปดาวน์ แล้วกด **▶ Run**
   - ครั้งแรกจะขึ้นขอสิทธิ์ (Authorize) → เลือกบัญชี Google ของคุณ → กด "Advanced" → "Go to (โปรเจกต์) (unsafe)" → Allow
   - หลังรันเสร็จ ชีตของคุณจะมีแท็บ **Members** พร้อมหัวตารางอัตโนมัติ
7. ตั้งรหัสผ่านสำหรับหน้าเจ้าหน้าที่ (admin):
   - ปุ่ม ▶ Run **เรียกฟังก์ชันโดยไม่มี argument ให้** ดังนั้นจะเลือก `setAdminPassword` แล้ว Run ตรง ๆ ไม่ได้ (จะขึ้น error "Invalid argument: value")
   - ให้เลื่อนไปท้ายไฟล์ `Code.gs` แล้วเพิ่มฟังก์ชันชั่วคราวนี้ต่อท้าย:
     ```js
     function runSetAdminPassword() {
       setAdminPassword("รหัสผ่านที่ต้องการ"); // เปลี่ยนข้อความในเครื่องหมายคำพูดเป็นรหัสผ่านจริง
     }
     ```
   - กด **บันทึก** (Ctrl+S)
   - ที่ดรอปดาวน์เลือกฟังก์ชัน เปลี่ยนจาก `setAdminPassword` เป็น **`runSetAdminPassword`** แล้วกด **▶ Run**
   - ครั้งแรกอาจขอสิทธิ์ (Authorize) อีกครั้ง → กด Allow
   - เมื่อรันสำเร็จ (ไม่มี error ในบันทึกการดำเนินการ) รหัสผ่านจะถูกเก็บใน Script Properties เรียบร้อย
   - หลังจากนั้นจะลบฟังก์ชัน `runSetAdminPassword` ทิ้ง หรือจะเก็บไว้เผื่อเปลี่ยนรหัสผ่านครั้งหน้าก็ได้ (แค่แก้ข้อความรหัสผ่านแล้ว Run ใหม่)
8. Deploy เป็น Web App:
   - มุมขวาบน กด **Deploy → New deployment**
   - เลือกรูปแบบ (gear icon) → **Web app**
   - Execute as: **Me**
   - Who has access: **Anyone**
   - กด **Deploy** → อนุญาตสิทธิ์อีกครั้งถ้าจำเป็น
   - คัดลอก **Web app URL** ที่ได้ (ลงท้ายด้วย `/exec`) — จะใช้ในขั้นถัดไป
9. ตั้งค่าลิงก์เว็บไซต์สำหรับอีเมลแจ้งผลอนุมัติ:
   - เลื่อนขึ้นไปใกล้ต้นไฟล์ `Code.gs` หาบรรทัด
     ```js
     const SITE_URL = "ใส่_URL_เว็บไซต์ของคุณที่นี่/";
     ```
   - แทนที่ด้วย URL เว็บไซต์จริงของคุณ (ได้จากขั้นตอนที่ 5 ในส่วนที่ 2 ด้านล่าง) เช่น
     ```js
     const SITE_URL = "https://cpc-psw-thailand.github.io/member/";
     ```
   - ใส่แบบไหนก็ได้ ระบบรองรับทั้ง URL โฟลเดอร์เว็บเฉย ๆ (`.../member/`) และ URL หน้า status.html เต็ม ๆ (`.../member/status.html`) จะไม่ต่อซ้ำกันเป็น `status.html/status.html`
   - ถ้ายังไม่มี URL ตอนนี้ ข้ามไปก่อนได้ แล้วค่อยกลับมาใส่ทีหลัง — ถ้าไม่ตั้งค่า อีเมลแจ้งผลจะบอกให้สมาชิกไปกรอกข้อมูลที่หน้าตรวจสอบสถานะเองแทนที่จะแนบลิงก์ตรง (ระบบยังทำงานได้ปกติ)
10. *(ทางเลือก แนะนำ)* ตั้งเวลาให้ระบบอัปเดตสมาชิกที่หมดอายุอัตโนมัติทุกวัน:
   - แถบซ้าย **Triggers (นาฬิกา)** → **Add Trigger**
   - Function: `updateExpiredMembers`, Event source: **Time-driven**, Type: **Day timer** → เลือกช่วงเวลา → Save

> ทุกครั้งที่แก้โค้ดใน Apps Script ต้องทำ **Deploy → Manage deployments → แก้ไข (ดินสอ) → New version → Deploy** ใหม่ ไม่เช่นนั้น URL เดิมจะยังใช้โค้ดเวอร์ชันเก่า

โค้ดเวอร์ชันล่าสุดเพิ่ม 2 ฟีเจอร์: **(1) สมาชิกวิสามัญขอเปลี่ยนเป็นสมาชิกสามัญได้เอง** — สมาชิกวิสามัญที่สถานะ "ใช้งานอยู่" จะเห็นฟอร์มขอเปลี่ยนประเภทในหน้าตรวจสอบสถานะ กรอกข้อมูลผู้ทำหน้าที่ ป.วิ.อาญา + แนบรูปบัตร ส่งแล้วเจ้าหน้าที่จะเห็นคำขอในหน้ารายละเอียดใบสมัคร กดอนุมัติ/ปฏิเสธได้ (คอลัมน์ใหม่: `UpgradeRequestDate`) **(2) ส่งข่าวสารถึงสมาชิกทางอีเมลจากหน้าเจ้าหน้าที่** — มีการเช็คโควต้าอีเมลคงเหลือของวันและจำกัดการส่งไม่เกิน 90% ของโควต้าที่เหลือ (สำรอง 10% ไว้ให้อีเมลระบบอื่น เช่น แจ้งผลอนุมัติ) ให้ทำดังนี้:

1. คัดลอกเนื้อหาไฟล์ `apps-script/Code.gs` ใหม่ทั้งหมดไปวางทับของเดิมใน Apps Script Editor แล้วบันทึก
2. **อย่าลืมตั้งค่า `SITE_URL` ใหม่อีกครั้ง** (การวางทับทั้งไฟล์จะรีเซ็ตค่านี้กลับเป็นค่าเริ่มต้นเสมอ — ดูขั้นตอนที่ 9 ด้านบน)
3. เลือกฟังก์ชัน `migrate_v2` จากดรอปดาวน์ แล้วกด **▶ Run** ครั้งเดียว (จะเพิ่มหัวตารางคอลัมน์ `UpgradeRequestDate` ให้อัตโนมัติ)
4. Deploy เวอร์ชันใหม่ (Manage deployments → New version → Deploy)
5. อัปโหลดไฟล์ที่เปลี่ยน/ใหม่ทับ GitHub: `status.html`, `status.js`, `admin.html`, `admin.js`, `style.css`
6. เช็คบน GitHub โดยตรงก่อน (Ctrl+F หาคำว่า "ส่งข่าวสารถึงสมาชิก") เพื่อยืนยันว่าอัปโหลดสำเร็จ ก่อนไปทดสอบที่หน้าเว็บจริง

> หมายเหตุเรื่องโควต้าอีเมล: Gmail ฟรีจำกัดที่ประมาณ 100 ฉบับ/วัน ระบบข่าวสารจะคำนวณจากโควต้าที่เหลือ ณ ขณะนั้น ไม่ใช่โควต้าเต็มของวัน (เช่น ถ้าวันนี้ส่งอีเมลระบบอื่นไปแล้ว 30 ฉบับ จะเหลือโควต้า 70 และระบบข่าวสารจะส่งได้สูงสุด 63 ฉบับ = 90% ของ 70) ถ้าสมาชิกมีจำนวนมากกว่าโควต้าที่ส่งได้ ระบบจะส่งเท่าที่ทำได้และแจ้งจำนวนที่เหลือ ให้กดส่งใหม่ในวันถัดไป

โค้ดเวอร์ชันล่าสุด (ถัดจากด้านบน) ยังปรับปรุงเพิ่มอีก 3 จุด:

1. **หน้าแก้ไขข้อมูลสมาชิกด้วยตนเอง** — เดิมมีเฉพาะตอนเจ้าหน้าที่ขอเอกสารเพิ่มเติม ตอนนี้สมาชิกแก้ไขข้อมูลตนเองได้ตลอดเวลาจากหน้าตรวจสอบสถานะ (ฟิลด์ครบเหมือนหน้าใบสมัคร พร้อมเติมค่าปัจจุบันให้อัตโนมัติ รวมถึงเลือกจังหวัด/อำเภอ/ตำบลที่เคยกรอกไว้)
2. **หน้าเจ้าหน้าที่แยกเป็น 2 แท็บ** — "แดชบอร์ด" (สถิติ + สัดส่วนวิชาชีพ + สัดส่วนประเภทสมาชิกใหม่) และ "ตรวจสอบ/จัดการสมาชิก" (ตาราง + ปุ่มจัดการต่าง ๆ) เพื่อความชัดเจน
3. ไม่มีการเปลี่ยนแปลงคอลัมน์ในชีต จึงไม่ต้องรัน `migrate_v2` เพิ่มสำหรับรอบนี้ — แค่อัปโหลดไฟล์ที่เปลี่ยน (`status.html`, `status.js`, `admin.html`, `admin.js`, `style.css`, `apps-script/Code.gs`) ทับ GitHub และ Deploy Apps Script เวอร์ชันใหม่ตามปกติ

โค้ดเวอร์ชันล่าสุด (ถัดจากด้านบนอีกที) แก้ไข/เพิ่มอีก 6 จุด:

1. **แก้บั๊กปุ่มกดไม่ได้ในหน้าเจ้าหน้าที่** — ปุ่ม "+ เพิ่มสมาชิกกิตติมศักดิ์" และ "+ ส่งข่าวสารถึงสมาชิก" เคยซ้ำกันอยู่ 2 จุด (ในแถบหัวข้อ และในแท็บ "ตรวจสอบ/จัดการสมาชิก") ทำให้ปุ่มที่อยู่ในแท็บกดไม่ได้ (เพราะ id ซ้ำกัน) ตอนนี้เหลือจุดเดียวและกดได้ปกติ
2. **"ตรวจสอบโควต้าอีเมลไม่สำเร็จ: ไม่รู้จักคำสั่งนี้"** — เกิดจาก Apps Script ที่ deploy อยู่ยังเป็นเวอร์ชันเก่า (ไม่มีคำสั่ง `adminGetEmailQuota`/`adminSendNewsletter`) โค้ดฝั่งนี้ถูกต้องอยู่แล้ว แค่ต้องคัดลอกไฟล์ `Code.gs` ใหม่ไปวางทับแล้ว Deploy เวอร์ชันใหม่ตามขั้นตอนด้านล่าง
3. **ระบุอีเมลเองได้** — เพิ่มตัวเลือก "ระบุอีเมลเอง" ในช่อง "ส่งถึง" ของฟอร์มส่งข่าวสาร กรอกอีเมลคั่นด้วยจุลภาคหรือขึ้นบรรทัดใหม่ได้หลายรายการ
4. **แนบรูปภาพในอีเมลข่าวสารได้** — เพิ่มช่องแนบรูปภาพ (สูงสุด 3 รูป ไฟล์ละไม่เกิน 3 MB) รูปจะแสดงต่อท้ายเนื้อหาข่าวสารในอีเมล
5. **อีเมลขอเอกสารเพิ่มเติมมีวิธีใช้แบบย่อ** — เพิ่มขั้นตอน 3 ข้อสั้น ๆ ในอีเมล อธิบายวิธีกดลิงก์ไปหน้าเว็บและแนบเอกสาร
6. **แก้บั๊กฟอร์มแก้ไขข้อมูลไม่แสดงคำนำหน้า/ชื่อ/นามสกุล/วันเกิดเดิม** — เสริมความทนทานของโค้ดฝั่ง Apps Script ให้แปลงวันเกิดเป็นรูปแบบที่ถูกต้องเสมอไม่ว่า Google ชีตจะเก็บเป็นข้อความหรือ Date object ก็ตาม (ถ้ายังไม่ขึ้นหลัง deploy เวอร์ชันนี้ ให้ตรวจสอบว่าอัปโหลดไฟล์ `Code.gs` ล่าสุดและ Deploy แล้วจริง ๆ ตามข้อ 2)

ให้ทำตามนี้:
1. คัดลอกเนื้อหาไฟล์ `apps-script/Code.gs` ใหม่ทั้งหมดไปวางทับของเดิมใน Apps Script Editor แล้วบันทึก
2. **อย่าลืมตั้งค่า `SITE_URL` ใหม่อีกครั้ง**
3. ไม่ต้องรัน `migrate_v2` (ไม่มีคอลัมน์ใหม่รอบนี้)
4. Deploy เวอร์ชันใหม่ (Manage deployments → New version → Deploy)
5. อัปโหลดไฟล์ที่เปลี่ยน: `admin.html`, `admin.js`
6. ตรวจสอบบน GitHub โดยตรงก่อน (Ctrl+F หาคำว่า "ระบุอีเมลเอง" ในไฟล์ `admin.html`) เพื่อยืนยันว่าอัปโหลดสำเร็จ แล้วค่อยทดสอบที่หน้าเว็บจริงด้วย Hard Refresh หรือ Incognito

โค้ดเวอร์ชันล่าสุด (ถัดจากด้านบนอีกที) เพิ่ม **จำนวนสมาชิกที่หน้าแรก** — แสดงจำนวนสมาชิกทั้งหมด/สมาชิกสามัญ/สมาชิกวิสามัญ (นับเฉพาะสถานะ "ใช้งานอยู่" เท่านั้น ไม่รวมที่ยังรอตรวจสอบ) เป็นข้อมูลสาธารณะที่ไม่ต้องล็อกอิน ให้ทำดังนี้:

1. คัดลอกเนื้อหาไฟล์ `apps-script/Code.gs` ใหม่ทั้งหมดไปวางทับของเดิมใน Apps Script Editor แล้วบันทึก (เพิ่มคำสั่งใหม่ `publicStats` ไม่มีคอลัมน์ชีตเปลี่ยนแปลง ไม่ต้องรัน `migrate_v2`)
2. **อย่าลืมตั้งค่า `SITE_URL` ใหม่อีกครั้ง**
3. Deploy เวอร์ชันใหม่ (Manage deployments → New version → Deploy)
4. อัปโหลดไฟล์ที่เปลี่ยน/ใหม่ทับ GitHub: `index.html`, `style.css`, และไฟล์ใหม่ **`index.js`**

โค้ดเวอร์ชันล่าสุด (ถัดจากด้านบนอีกที) เพิ่ม **ระบบข่าวสาร** — หน้าเว็บสาธารณะสำหรับลงข่าวประชาสัมพันธ์ถึงสมาชิกและประชาชนทั่วไป พร้อมภาพ thumbnail และภาพประกอบเนื้อหา และระบบจัดการหลังบ้านในเมนูเจ้าหน้าที่ ให้ทำดังนี้:

1. คัดลอกเนื้อหาไฟล์ `apps-script/Code.gs` ใหม่ทั้งหมดไปวางทับของเดิมใน Apps Script Editor แล้วบันทึก
2. **อย่าลืมตั้งค่า `SITE_URL` ใหม่อีกครั้ง**
3. เลือกฟังก์ชัน `getNewsSheet_` **ไม่ต้องรัน** — ชีตแท็บ "News" จะถูกสร้างอัตโนมัติเองในการเรียกใช้งานครั้งแรก (ตอนกดเข้าแท็บ "ข่าวสาร" ในหน้าเจ้าหน้าที่ หรือเปิดหน้า news.html ครั้งแรก) ไม่ต้องตั้งค่าอะไรเพิ่มในชีต
4. Deploy เวอร์ชันใหม่ (Manage deployments → New version → Deploy)
5. อัปโหลดไฟล์ที่เปลี่ยน/ใหม่ทับ GitHub: `index.html`, `register.html`, `status.html`, `admin.html` (เพิ่มลิงก์เมนู "ข่าวสาร"), `admin.js`, `style.css`, และไฟล์ใหม่ **`news.html`**, **`news.js`**

**วิธีใช้งานระบบข่าวสาร:**
- ไปที่หน้าเจ้าหน้าที่ → แท็บ **"ข่าวสาร"** → กด **"+ เพิ่มข่าวสาร"**
- กรอกหัวข้อ สรุปย่อ (แสดงในหน้ารวมข่าว) และเนื้อหาข่าวเต็ม
- แนบภาพ **Thumbnail** (บังคับ — ใช้เป็นภาพหลักทั้งในหน้ารวมข่าวและหน้ารายละเอียด) และภาพประกอบเนื้อหาเพิ่มเติมได้สูงสุด 3 รูป (ไม่บังคับ)
- เลือกสถานะ **"เผยแพร่ทันที"** ให้ขึ้นหน้าเว็บทันที หรือ **"บันทึกเป็นฉบับร่าง"** เพื่อเก็บไว้แก้ไขต่อภายหลังโดยยังไม่แสดงต่อสาธารณะ
- แก้ไข/ลบข่าวได้จากตารางรายการข่าวในแท็บเดียวกัน — ตอนแก้ไข ถ้าไม่อัปโหลดรูปใหม่ระบบจะใช้รูปเดิมต่อ
- ผู้เข้าชมทั่วไปดูข่าวได้ที่เมนู **"ข่าวสาร"** บนเว็บ ไม่ต้องล็อกอิน

> หมายเหตุ: รูปข่าวจะถูกเก็บในโฟลเดอร์ Google Drive แยกต่างหากชื่อ **"CPC-PSW News Images"** (สร้างอัตโนมัติ) ไม่ปะปนกับเอกสารของสมาชิก

### ถ้าเคยตั้งค่าระบบนี้มาก่อนหน้านี้แล้ว (มีชีตอยู่แล้ว) — อัปเดตก่อนหน้า

โค้ดเวอร์ชันก่อนหน้าเพิ่ม: การส่งอีเมลแจ้งผลอนุมัติ/ต่ออายุพร้อมลิงก์ดูบัตรสมาชิก, ระบบต่ออายุสมาชิกภาพด้วยตนเอง, บัตรสมาชิกอิเล็กทรอนิกส์ (e-Card), และแก้ปัญหาเลข 0 นำหน้าเบอร์โทร/รหัสไปรษณีย์หายไปให้แน่นหนาขึ้นอีกชั้น ให้ทำดังนี้:

> หมายเหตุ: ถ้าเคยตั้งค่า `SITE_URL` เป็นลิงก์ status.html เต็ม ๆ ไว้แล้ว (ทำให้อีเมลก่อนหน้านี้มีลิงก์ซ้ำเป็น `status.html/status.html?...`) ไม่ต้องแก้ค่า `SITE_URL` ก็ได้ เวอร์ชันนี้แก้ให้รองรับทั้งสองแบบแล้ว แค่ทำตามขั้นตอนด้านล่างเพื่ออัปเดตโค้ดให้ครบก็พอ

1. คัดลอกเนื้อหาไฟล์ `apps-script/Code.gs` ใหม่ทั้งหมดไปวางทับของเดิมใน Apps Script Editor แล้วบันทึก
2. ตั้งค่า `SITE_URL` ตามขั้นตอนที่ 9 ด้านบน (ถ้ายังไม่เคยตั้ง)
3. เลือกฟังก์ชัน `migrate_v2` จากดรอปดาวน์ แล้วกด **▶ Run** ครั้งเดียว
4. **แถวข้อมูลเดิมที่เลข 0 นำหน้าหายไปแล้ว** (เช่น เบอร์โทรที่บันทึกเป็น 639429663 แทนที่จะเป็น 0639429663) ต้องแก้ไขด้วยตนเองในชีต: คลิกที่เซลล์นั้น พิมพ์เบอร์ใหม่ให้ถูกต้อง — เวอร์ชันนี้บังคับฟอร์แมตข้อความให้กับทุกแถวใหม่ตอนบันทึกโดยตรง (ไม่ต้องพึ่งฟอร์แมตที่ตั้งไว้ล่วงหน้าอย่างเดียวอีกต่อไป) จึงมั่นใจได้ว่าแถวใหม่จากนี้จะไม่มีปัญหานี้อีก
5. ถ้าเคยตั้งสิทธิ์ **Gmail/MailApp** ไว้แล้วไม่ต้องขอใหม่ แต่ถ้าเป็นครั้งแรกที่ระบบพยายามส่งอีเมล (ตอนกดอนุมัติสมาชิก) อาจมีป๊อปอัปขอสิทธิ์เพิ่มเติม ให้กด Allow
6. Deploy เวอร์ชันใหม่ตามขั้นตอนด้านบน (Manage deployments → New version → Deploy) แล้วอัปเดตไฟล์เว็บที่เปลี่ยนทั้งหมด (`status.html`, `status.js`, `admin.html`, `admin.js`, `style.css`) ขึ้น GitHub ทับของเดิม

---

## ส่วนที่ 2 — ตั้งค่าไฟล์เว็บและอัปโหลดขึ้น GitHub Pages

1. เปิดไฟล์ `config.js`
2. แก้บรรทัด
   ```js
   const APPS_SCRIPT_URL = "PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE";
   ```
   ให้เป็น URL ที่คัดลอกจากขั้นตอนที่ 7 ด้านบน เช่น
   ```js
   const APPS_SCRIPT_URL = "https://script.google.com/macros/s/AKfycb.../exec";
   ```
3. สร้าง repository ใหม่บน GitHub เช่น `cpc-psw-membership`
4. อัปโหลดไฟล์ทั้งหมดในโปรเจกต์นี้ขึ้น repo (ยกเว้นโฟลเดอร์ `apps-script/` ไม่จำเป็นต้องอยู่บนเว็บ แต่ใส่ไว้ก็ไม่เสียหาย เพราะเป็นแค่ไฟล์ .gs เฉย ๆ ไม่ถูกรันบนเว็บ)
   - ⚠️ **สำคัญ**: ทุกไฟล์ (`.html`, `.js`, `.css`, `logo.png`) ต้องอยู่ที่ **root ของ repo** เสมอ ห้ามมีโฟลเดอร์ย่อยคั่นกลาง เพราะไฟล์ HTML อ้างอิงพาธแบบ `style.css`, `config.js` ตรง ๆ (ไม่มี `assets/`) ถ้าใช้ปุ่ม **Add file → Upload files** บนเว็บ ให้ลาก **ไฟล์แต่ละไฟล์** (ไม่ใช่ลากทั้งโฟลเดอร์ที่มีไฟล์ซ้อนอยู่ข้างใน) เข้าไปวางที่หน้า root โดยตรง
   - หรือใช้คำสั่ง:
     ```bash
     cd cpc-psw-web
     git init
     git add .
     git commit -m "Initial CPC-PSW membership system"
     git branch -M main
     git remote add origin https://github.com/<username>/cpc-psw-membership.git
     git push -u origin main
     ```
5. เปิดใช้งาน GitHub Pages:
   - ไปที่ repo → **Settings → Pages**
   - Source: **Deploy from a branch**
   - Branch: **main**, Folder: **/(root)**
   - กด **Save**
   - รอ 1–2 นาที จะได้ URL เว็บไซต์ เช่น `https://<username>.github.io/cpc-psw-membership/`

6. ทดสอบระบบ:
   - เปิดเว็บ → ไปหน้า **สมัครสมาชิก** → กรอกข้อมูลทดสอบ → ส่งใบสมัคร
   - เปิด Google ชีต ตรวจว่ามีแถวข้อมูลใหม่ในแท็บ Members
   - ไปหน้า **สำหรับเจ้าหน้าที่** → ใส่รหัสผ่านที่ตั้งไว้ → ต้องเห็นรายการสมัคร → กด **อนุมัติ**
   - ไปหน้า **ตรวจสอบสถานะ** → กรอกเลขบัตรประชาชน/เบอร์เดียวกับที่สมัคร → ต้องเห็นรหัสสมาชิกและสถานะ "ใช้งานอยู่"

---

## ข้อจำกัดและคำแนะนำด้านความปลอดภัย

- รหัสผ่านหน้าเจ้าหน้าที่เป็นการป้องกันขั้นพื้นฐาน (ส่งรหัสผ่านไปตรวจกับ Apps Script ทุกครั้งที่เรียกใช้งาน) เหมาะกับทีมงานขนาดเล็กที่ไว้ใจกัน **ไม่ใช่ระบบยืนยันตัวตนระดับสูง** หากต้องการความปลอดภัยมากขึ้น ควรเปลี่ยนไปใช้ Google OAuth (Google Sign-In) และตรวจสอบอีเมลผู้ใช้ในฝั่ง Apps Script แทน
- เลขบัตรประชาชนที่จัดเก็บในชีตยังเป็นข้อความปกติ (ไม่ได้เข้ารหัส) ควรจำกัดสิทธิ์การเข้าถึงชีตต้นฉบับให้เฉพาะเจ้าหน้าที่ที่เกี่ยวข้อง (Share เฉพาะอีเมลที่จำเป็น) และเปิด 2-Step Verification ของบัญชี Google ที่ดูแลระบบ
- Google Apps Script Web App ฟรีมีโควตาการเรียกใช้งานต่อวัน (รายละเอียดเปลี่ยนแปลงได้ตามนโยบาย Google) หากมีผู้ใช้จำนวนมากควรพิจารณาอัปเกรดเป็นระบบฐานข้อมูลแบบอื่นในอนาคต

## ฟีเจอร์ในเวอร์ชันนี้

- **อีเมลแจ้งผลอนุมัติ/ต่ออายุอัตโนมัติ**: เมื่อเจ้าหน้าที่กด "อนุมัติ" ระบบจะส่งอีเมล (จัดหน้าสวยงามแบบ HTML พร้อมปุ่มกดไปยังบัตรสมาชิก) แจ้งรหัสสมาชิก วันหมดอายุ และลิงก์ตรงไปยังบัตรสมาชิกอิเล็กทรอนิกส์ (ถ้าตั้งค่า `SITE_URL` ไว้แล้ว) หากส่งอีเมลไม่สำเร็จ (เช่น เกินโควตารายวันของ Gmail) การอนุมัติจะยังสำเร็จตามปกติ เพียงแต่จะไม่มีอีเมลออกไป
- **บัตรสมาชิกอิเล็กทรอนิกส์ (e-Card)**: หน้า "ตรวจสอบสถานะ" จะแสดงบัตรสมาชิกแบบการ์ดสวยงามโดยอัตโนมัติเมื่อสถานะเป็น "ใช้งานอยู่" พร้อมปุ่ม **"บันทึกภาพบัตรสมาชิก"** ให้สมาชิกดาวน์โหลดเป็นไฟล์ .png เก็บไว้ในมือถือได้ (ใช้ไลบรารี html2canvas)
- **ต่ออายุสมาชิกภาพด้วยตนเอง**: เมื่อสถานะเป็น "หมดอายุ" หน้าตรวจสอบสถานะจะมีปุ่ม "ต่ออายุสมาชิกภาพ" ให้สมาชิกกดส่งคำขอเอง ระบบจะเปลี่ยนสถานะเป็น "รอตรวจสอบ" ให้เจ้าหน้าที่พิจารณาอนุมัติอีกครั้ง (เมื่ออนุมัติ รหัสสมาชิกเดิมจะถูกใช้ต่อ ไม่ออกรหัสใหม่)
- **ขอเอกสาร/ข้อมูลเพิ่มเติมจากผู้สมัคร**: ในหน้าเจ้าหน้าที่ กดปุ่ม "ขอเอกสารเพิ่มเติม" ที่ใบสมัครสถานะ "รอตรวจสอบ" พิมพ์ข้อความระบุสิ่งที่ต้องการ ระบบจะส่งอีเมลแจ้งผู้สมัครพร้อมลิงก์ ผู้สมัครเข้าไปเห็นข้อความ แก้ไขข้อมูล (เบอร์โทร/อีเมล/ที่อยู่/ข้อมูลวิชาชีพ/ข้อมูล ป.วิ.อาญา) และแนบไฟล์เอกสารเพิ่มเติมได้เองที่หน้าตรวจสอบสถานะ เมื่อส่งแล้วใบสมัครจะกลับเข้าคิว "รอตรวจสอบ" อัตโนมัติ
- **สมาชิกวิสามัญขอเปลี่ยนเป็นสมาชิกสามัญได้เอง**: สมาชิกวิสามัญที่สถานะ "ใช้งานอยู่" จะเห็นฟอร์มขอเปลี่ยนประเภทใต้บัตรสมาชิกในหน้าตรวจสอบสถานะ กรอกตำแหน่ง/เลขทะเบียน/ประสบการณ์ทำงาน ป.วิ.อาญา พร้อมแนบรูปบัตรผู้ทำหน้าที่ฯ ส่งแล้วเจ้าหน้าที่จะเห็นคำขอในหน้ารายละเอียดใบสมัคร (มีป้าย "ขอเปลี่ยนเป็นสามัญ" กำกับในตารางด้วย) กดอนุมัติจะเปลี่ยนประเภทเป็นสมาชิกสามัญทันที หรือปฏิเสธเพื่อคงสถานะวิสามัญไว้
- **ส่งข่าวสารถึงสมาชิกทางอีเมล**: ในหน้าเจ้าหน้าที่ กดปุ่ม "+ ส่งข่าวสารถึงสมาชิก" เลือกกลุ่มเป้าหมาย (เฉพาะที่ใช้งานอยู่ หรือทุกคน) กรอกหัวข้อและเนื้อหา ระบบจะตรวจสอบโควต้าอีเมลคงเหลือของวันนั้นและ**จำกัดการส่งไม่เกิน 90% ของโควต้าที่เหลือ** (สำรอง 10% ไว้ให้อีเมลระบบอื่น เช่น แจ้งผลอนุมัติ/ขอเอกสารเพิ่มเติม) หากสมาชิกมีมากกว่าจำนวนที่ส่งได้ ระบบจะแจ้งจำนวนที่ส่งสำเร็จและจำนวนที่เหลือให้ส่งใหม่วันถัดไป
- โควตาการส่งอีเมลของ Gmail ฟรีอยู่ที่ประมาณ 100 ฉบับ/วัน ถ้าใช้ Google Workspace จะได้โควตาสูงกว่านี้

## แนวทางขยายระบบในอนาคต

- แจ้งเตือนอัตโนมัติก่อนหมดอายุ 60/15 วัน: เพิ่มฟังก์ชันที่รันคู่กับ trigger `updateExpiredMembers` เพื่อส่งอีเมลเตือนล่วงหน้าด้วย `MailApp.sendEmail()`
- เชื่อมระบบยืนยันตัวตนเจ้าหน้าที่ด้วย Google OAuth แทนรหัสผ่านเดียว เพื่อความปลอดภัยที่สูงขึ้นและแยกสิทธิ์รายบุคคล
- Export รายชื่อสมาชิกเป็น PDF/Excel สำหรับรายงานประจำปี
